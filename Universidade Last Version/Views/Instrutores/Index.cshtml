@model IEnumerable<Instrutores>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.InstrutoresNome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sobrenome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.InstrutoresNome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sobrenome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.InstrutoresID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.InstrutoresID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.InstrutoresID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
